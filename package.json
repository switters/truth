{
  "name": "ssot",
  "version": "0.0.7",
  "description": "Small file that establishes a single-source of truth across .env file, cli vars, and cli args.",
  "main": "index.js",
  "repository": "switters/ssot",
  "scripts": {
    "test": "echo \"Apologies - tests wip\" && exit 1"
  },
  "keywords": [
    "truth",
    "env",
    "vars",
    "arguments",
    "conf",
    "yargs",
    "config",
    "dotenv"
  ],
  "author": "switters",
  "license": "MIT",
  "dependencies": {
    "config": "^1.26.1",
    "dotenv": "^4.0.0",
    "yargs": "^8.0.1"
  }
}
