{
  "name": "ssot",
  "version": "0.0.2",
  "description": "Small file that establishes a single-source of truth across .env file, cli vars, and cli args.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "truth",
    "env",
    "vars",
    "arguments",
    "conf"
  ],
  "author": "switters <codenomadic@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "config": "^1.26.1",
    "dotenv": "^4.0.0",
    "yargs": "^8.0.1"
  }
}
